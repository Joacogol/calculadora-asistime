claude-agent-sdk>=0.1.0
google-api-python-client>=2.140.0
google-auth>=2.34.0
playwright>=1.47.0
jinja2>=3.1.0
Pillow>=10.4.0
numpy>=1.26.0
requests>=2.32.0

# Transcripción de voz para los subtítulos automáticos de los reels.
# Corre local: no se paga por minuto y ningún audio de un cliente sale
# hacia afuera. Suma unos 460 MB de modelo la primera vez que se usa,
# que se descargan al arrancar y quedan en la caché del contenedor.
faster-whisper>=1.0

# Encuadre por caras al pasar un video apaisado a reel (motor/encuadre.py).
# La versión headless no arrastra ninguna librería gráfica; el modelo de
# caras (230 KB) va versionado en motor/assets/, así que no se baja nada.
opencv-python-headless>=4.10
